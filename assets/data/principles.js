window.CCAF_PRINCIPLES = [
  {"id":"d1-p1","domain":"D1","title":"Agentic search over static context","explanation":"When the failure is a missing-context problem (e.g., a caller in an unchanged file isn't included in a static prompt), the fix is to turn the task into an agentic one where the model can read files and search the codebase via tools — not add chain-of-thought instructions or expand the prompt with static analysis."},
  {"id":"d1-p2","domain":"D1","title":"Decomposing open-ended tasks","explanation":"Use Glob and Grep to map the codebase structure, prioritize high-impact and heavily-coupled areas, create a plan, and revise it as new dependencies are discovered — don't read every file exhaustively, split effort evenly by directory, or proceed arbitrarily."},
  {"id":"d1-p3","domain":"D1","title":"Multi-phase workflows (analyze → propose → implement → review)","explanation":"This structure is most valuable for open-ended, judgment-heavy requests (e.g., \"improve error handling\"). Mechanical, well-defined tasks (e.g., a straightforward rename) gain little from the extra phases."},
  {"id":"d1-p4","domain":"D1","title":"Three decomposition patterns: prompt chaining, routing, orchestrator-workers","explanation":"Prompt chaining fits a fixed, identical sequence of subtasks every time. Routing fits inputs that fall into distinct categories needing different handling. Orchestrator-workers fits subtasks that vary unpredictably per input and must be decided dynamically by a central LLM."},
  {"id":"d1-p5","domain":"D1","title":"Restoring state after a crash","explanation":"Have each agent persist a structured export to a known location; on resume, the coordinator loads a manifest and injects only the relevant state into each subagent's prompt — don't rely on a full raw conversation log (too much context) or semantic vector search (lossy, may miss findings)."},
  {"id":"d1-p6","domain":"D1","title":"True parallel subagent execution","explanation":"The coordinator must emit multiple Task tool calls within a single assistant message — that's the only mechanism that makes the harness run subagents concurrently. Switching to a cheaper model, adding external async infrastructure, or simply instructing the model to \"run in parallel\" does not guarantee this."},
  {"id":"d1-p7","domain":"D1","title":"\"Task\" must be in allowedTools","explanation":"If a coordinator with fully configured AgentDefinitions reasons about delegating but no subagent ever actually executes (with no errors logged), the most likely cause is that \"Task\" is missing from allowedTools — the model can talk about delegation but has no mechanism to invoke it."},
  {"id":"d1-p8","domain":"D1","title":"Flat, non-recursive hierarchies","explanation":"To cut latency while keeping monitoring and debugging centralized, have the coordinator itself spawn parallel subagents and aggregate results — avoid nested/recursive agent hierarchies (subagents spawning their own subagents), which severely hurt observability."},
  {"id":"d1-p9","domain":"D1","title":"Preserve attribution with structured data","explanation":"To avoid losing which claim came from which source during synthesis, require subagents to emit explicit structured claim-source mappings that the synthesis agent must preserve — not inline text-prefix conventions, post-hoc log analysis, or unreliable semantic-similarity reconstruction."},
  {"id":"d1-p10","domain":"D1","title":"Closed feedback loops for gap detection","explanation":"When an analysis agent identifies a specific gap (e.g., \"missing details on token refresh\"), it should report that gap to the coordinator, which triggers targeted follow-up searches and re-runs analysis until coverage is sufficient — not just attach a confidence score and stop, or add an upfront planning agent."},
  {"id":"d1-p11","domain":"D1","title":"Passing context between subagents","explanation":"Use reference identifiers pointing to a shared store rather than inlining full raw content into the prompt (bloats context, risks information loss) or relying on callbacks/automatic context inheritance (neither exists in this architecture)."},
  {"id":"d1-p12","domain":"D1","title":"Format-aware synthesis","explanation":"The synthesis agent should render each content type in its natural form — tables for financial comparisons, prose for news narratives — rather than flattening everything into one uniform format like bullet points."},
  {"id":"d1-p13","domain":"D1","title":"Dynamic, LLM-judged query routing","explanation":"With a diverse and evolving query distribution, let the coordinator's LLM judgment decide dynamically how many and which subagents to invoke per query — static fast-paths, hand-coded pattern routing, and a classifier requiring periodic retraining all break down as patterns evolve."},
  {"id":"d1-p14","domain":"D1","title":"Delegate with goals, not scripts","explanation":"Specify research goals and quality criteria (coverage breadth, source diversity, recency) rather than prescriptive step-by-step instructions — this lets a capable subagent adapt its own strategy instead of failing rigidly when a pre-specified approach doesn't work."},
  {"id":"d1-p15","domain":"D1","title":"Purpose-specific tools over one generic tool","explanation":"When a single free-text tool produces inconsistent output formats across different task types, split it into narrowly-scoped tools with explicit input/output contracts rather than adding an enum parameter, better descriptions, or pre-classification."},
  {"id":"d1-p16","domain":"D1","title":"No automatic context sharing between subagent calls","explanation":"Each subagent invocation is a stateless API call with its own isolated context — the coordinator must explicitly inject the prior agents' outputs into the next subagent's prompt, or that subagent will report having no information at all."},
  {"id":"d1-p17","domain":"D1","title":"Don't spawn a subagent when the coordinator already has the context","explanation":"If the coordinator already holds all the needed findings in its own context (e.g., from orchestrating prior research), handle a simple follow-up directly rather than spawning a subagent and re-transferring tens of thousands of tokens — that's pure latency overhead."},
  {"id":"d1-p18","domain":"D1","title":"A structured source index for the report generator","explanation":"Pass the synthesis draft together with a structured source index mapping key claims to their source URLs and excerpts — this is the best balance of completeness and efficiency, better than passing all raw content, name-only attribution, or post-hoc citation matching."},
  {"id":"d1-p19","domain":"D1","title":"The coordinator is always the hub","explanation":"In an orchestrator-worker pattern, subagents never call each other directly, and there's no event bus or automatic sibling access — all information flows through the coordinator, which explicitly forwards relevant output into the next subagent's prompt."},
  {"id":"d1-p20","domain":"D1","title":"Include timestamps/metadata in structured output","explanation":"If two sources report different numbers over time and the synthesis agent wrongly flags them as \"contradictory,\" the fix is requiring every data point to carry its own publication or collection date — that lets synthesis reason about trends instead of conflicts."},
  {"id":"d1-p21","domain":"D1","title":"Handling uncertainty through report structure","explanation":"Preserve the original source characterization and methodological context, and structure the report to separate well-established findings from contested ones — don't average conflicting estimates that use different methodologies, silently filter out uncertain data, or require a rigid two-source minimum."},
  {"id":"d2-p1","domain":"D2","title":"Choosing the right built-in tool","explanation":"Grep searches file contents for a pattern — this is the right choice whenever the task is about content, not filenames. Glob matches file names/paths, Read opens a known file, and Bash executes commands."},
  {"id":"d2-p2","domain":"D2","title":"Use an existing MCP server for standard services","explanation":"For data from a common third-party service (like Jira), integrate an existing MCP server, which provides discoverable, typed tools with managed authentication — building a custom server, exporting to markdown, or using ad-hoc curl calls are all worse fits when a standard integration already exists."},
  {"id":"d2-p3","domain":"D2","title":"MCP Resources reduce exploratory tool-call round trips","explanation":"When an agent makes 8-10 sequential tool calls because it has no visibility into what each server contains, expose each server's content catalog as MCP resources so the agent can browse before acting — don't consolidate servers into a monolith or add keyword-based routing."},
  {"id":"d2-p4","domain":"D2","title":"Fewer tools improves tool selection accuracy","explanation":"Tool selection accuracy degrades as the number of available (especially irrelevant) tools grows. Scope each specialized subagent to only 4-5 role-relevant tools rather than exposing all tools to every agent."},
  {"id":"d2-p5","domain":"D2","title":"Stale tool results in a resumed session","explanation":"When resuming an old session whose tool results are now outdated, start a fresh session, inject a structured summary of the prior interaction, and make fresh tool calls before responding — don't strip tool_results from mid-transcript or just instruct the model to \"prefer the latest result.\""},
  {"id":"d2-p6","domain":"D2","title":"Escalation logic based on natural-language judgment","explanation":"The most reliable escalation criteria are: the customer explicitly requests a human, the issue requires a policy exception, or the agent cannot make meaningful progress — this outperforms a hard retry-count threshold, sentiment analysis, or a rigid rules engine."},
  {"id":"d2-p7","domain":"D2","title":"A structured handoff for human agents","explanation":"When escalating, pass a structured summary — customer ID, root cause, amount, and recommended action — not the raw transcript or just the customer's original complaint, so the human can act immediately without re-investigating."},
  {"id":"d2-p8","domain":"D2","title":"Compliance-critical rules require a deterministic hook, not a prompt","explanation":"If a rule \"cannot be left to model discretion\" (e.g., refunds over $500 must always escalate), enforce it with a hook that intercepts the tool call and blocks it — even a strongly worded system prompt still has a nonzero failure rate and is not compliance-grade."},
  {"id":"d2-p9","domain":"D2","title":"Graceful degradation when a tool fails","explanation":"When a backend action fails but other information is already known, explain what's understood, acknowledge the failure honestly, and offer the customer a choice — don't promise an outcome that hasn't happened, and don't escalate immediately while ignoring the value already delivered."},
  {"id":"d2-p10","domain":"D2","title":"Respect the customer's stated preference","explanation":"If a frustrated customer explicitly asks for a human but the issue is actually simple and resolvable in one tool call, acknowledge the frustration, state that it can be resolved now, and let the customer choose — don't unilaterally act against their stated preference."},
  {"id":"d2-p11","domain":"D2","title":"The API is stateless","explanation":"If the agent \"forgets\" information mid-flow, the cause is almost always that the full conversation history wasn't included in the latest request — not a missing \"remember this\" instruction, and not some default two-turn memory limit (no such limit exists)."},
  {"id":"d2-p12","domain":"D2","title":"The standard MCP error pattern","explanation":"Return the error message in the tool result's content field with isError set to true, ideally enriched with a structured errorCategory and an isRetryable flag — not a thrown exception, not a silent empty result, and not an ad-hoc \"status\" field."},
  {"id":"d2-p13","domain":"D2","title":"The agentic loop reasons turn by turn","explanation":"The model decides its next action by reasoning over the tool_result just appended to the conversation — there is no implicit orchestration layer routing by field value, no hard-coded decision tree, and no pre-planned sequence of tool calls fixed at the start."},
  {"id":"d2-p14","domain":"D2","title":"Managing context in long, multi-issue conversations","explanation":"Summarize resolved topics into a narrative description while keeping full message history only for the currently active issue — a plain sliding window risks silently dropping an earlier issue the customer later asks about again."},
  {"id":"d2-p15","domain":"D2","title":"Pruning bulky tool output","explanation":"When a tool response includes 40+ fields, extract only the fields relevant to the current task and drop the rest — this is more reliable than paraphrasing into prose or standing up a vector database."},
  {"id":"d3-p1","domain":"D3","title":"Cap iterations and spend with native Claude Code flags","explanation":"--max-turns caps the number of agentic iterations and --max-budget-usd caps the dollar spend per invocation, both enforced natively by Claude Code in headless (claude -p) mode — a CI job timeout or a passive dashboard only monitors from the outside and doesn't enforce either limit directly."},
  {"id":"d3-p2","domain":"D3","title":"CLAUDE.md shapes output quality at the source","explanation":"To reduce low-value generated output, document team standards, fixture conventions, and good-vs-bad examples in CLAUDE.md — fixing the root cause at generation time beats filtering bad output after the fact."},
  {"id":"d3-p3","domain":"D3","title":"--resume <name> vs. --continue","explanation":"--resume session-name loads that exact named session with all its accumulated context. --continue only resumes the most recent conversation, which may not be the one you want if you've worked on other things since."},
  {"id":"d3-p4","domain":"D3","title":"Tool descriptions are the primary lever for tool selection","explanation":"If an agent keeps using a built-in tool instead of a specialized MCP tool, the root cause is usually a vague MCP tool description — write a detailed description of capabilities, outputs, and when to use it, rather than removing built-in tools or adding brittle routing rules."},
  {"id":"d3-p5","domain":"D3","title":"Functions re-exported under different names","explanation":"Before grepping for every caller of a function that's re-exported through wrapper modules under different names, first read the library and wrappers to enumerate every exposed alias — grepping only the original name will miss aliased callers."},
  {"id":"d3-p6","domain":"D3","title":"Resuming an interrupted subagent with a small update","explanation":"When an exploration session is interrupted and a small change occurred in the meantime, resume from the saved transcript and explicitly inform the agent of that specific change — don't restart from scratch, and don't resume silently while ignoring the change."},
  {"id":"d3-p7","domain":"D3","title":"Context degradation in long sessions","explanation":"When responses become generic instead of referencing the specific classes/functions discovered earlier, summarize the key findings, then spawn a subagent for the next task with that summary as its starting context — don't just /clear or keep going in the degraded context."},
  {"id":"d3-p8","domain":"D3","title":"Building understanding of a large module","explanation":"Identify the base class/interface first, read it to understand the shared abstraction, then trace the specific implementations relevant to the task — more effective than reading every file sequentially, prioritizing by file size, or only grepping keywords."},
  {"id":"d3-p9","domain":"D3","title":"fork_session for independent parallel exploration","explanation":"To compare two approaches starting from the same analysis baseline, use fork_session to create separate branches — this avoids exploring sequentially in one thread or manually recreating context in fresh sessions."},
  {"id":"d3-p10","domain":"D3","title":"Files changed during a session gap","explanation":"Resume the session (keeps efficiency) and explicitly tell the agent which specific files changed for targeted re-reading (keeps accuracy) — don't re-read everything, don't start fresh, and don't resume silently."},
  {"id":"d3-p11","domain":"D3","title":"A scratchpad file for durable \"memory\"","explanation":"When an agent starts giving inconsistent answers after 30+ minutes of exploration, have it maintain a scratchpad file recording key findings and reference it going forward — more stable than switching to a bigger model, clearing context on a timer, or pre-summarizing every source file upfront."},
  {"id":"d3-p12","domain":"D3","title":"Subagents for deep dives, main agent keeps the overview","explanation":"When a codebase is too large to fit in one context window, delegate well-scoped questions to subagents with fresh context while the main agent retains the high-level architectural understanding — this is the pattern for scaling exploration beyond a single context window."},
  {"id":"d3-p13","domain":"D3","title":"Debugging is adaptive, not pre-planned","explanation":"Let the agent dynamically generate its next investigation step based on what it just discovered — planning the entire path before any exploration or fanning out parallel workers across every layer when you don't yet know which layer is involved both waste effort."},
  {"id":"d3-p14","domain":"D3","title":"When Edit fails on a non-unique old_string","explanation":"In a file with repetitive patterns, the reliable fallback is: Read the whole file, make the change in memory, then Write it back — not a longer match string, not replace_all, and not appending to the end."},
  {"id":"d3-p15","domain":"D3","title":"Exploring an unfamiliar, large codebase","explanation":"Use Grep to find concrete entry points and trace outward following real import/function-call edges incrementally — reading every file containing broad keywords across 800+ files drowns the context in noise."},
  {"id":"d3-p16","domain":"D3","title":"A localized bug doesn't need Plan Mode","explanation":"When a stack trace already points to a specific area, go straight to direct execution — read the trace, read the relevant code, fix the root cause once found. Plan Mode is reserved for genuinely open-ended or architecturally complex problems."},
  {"id":"d4-p1","domain":"D4","title":"Separate \"find everything\" from \"filter it\"","explanation":"An instruction like \"only report high-confidence issues\" is followed literally and kills recall. Instead, split the process into a finding stage where the goal is coverage, and a separate downstream stage that applies the threshold."},
  {"id":"d4-p2","domain":"D4","title":"Batch API vs. the real-time Messages API","explanation":"The Batch API is 50% cheaper but asynchronous (up to a 24-hour window) — appropriate only when that latency remains acceptable for your workflow. Time-sensitive work with a tight SLA must use the real-time Messages API instead."},
  {"id":"d4-p3","domain":"D4","title":"Few-shot examples fix extraction consistency","explanation":"When compound phrases are split inconsistently, array lengths vary wildly, or output formats differ each time, add few-shot examples demonstrating the exact canonical format you want — more effective than hard count caps, a bigger model, or setting temperature to 0."},
  {"id":"d4-p4","domain":"D4","title":"Check accuracy by segment before automating","explanation":"Aggregate accuracy can hide a document type or field where accuracy is much lower. Always analyze accuracy by document type and field before automating decisions based on a confidence threshold."},
  {"id":"d4-p5","domain":"D4","title":"Schema design for versioned or conflicting values","explanation":"For fields that can be amended over time, capture multiple values each with a source location and effective date — don't force one value, hard-code \"the latest always wins,\" or destructively strip superseded sections."},
  {"id":"d4-p6","domain":"D4","title":"Retry-with-error-feedback cannot invent missing information","explanation":"Retries reliably fix format and structural mistakes the model could have gotten right. They cannot fix a case where the needed information genuinely doesn't exist in the input."},
  {"id":"d4-p7","domain":"D4","title":"Explicitly instruct \"return null, don't guess\"","explanation":"Even nullable fields can still get a plausible-but-wrong value hallucinated by the model. Add an explicit instruction to return null when information isn't stated — don't make the field required."},
  {"id":"d4-p8","domain":"D4","title":"Few-shot examples across varied real-world formats","explanation":"When the model misses information that's genuinely present but presented in an unfamiliar structure, few-shot examples spanning that format variety raise recall — regex post-processing and retries don't teach the model to recognize new structures."},
  {"id":"d4-p9","domain":"D4","title":"Surface discrepancies instead of silently fixing them","explanation":"Add a calculated field alongside the stated field, and flag records where they differ for human review — don't have the model or a second pass silently reconcile financial data."},
  {"id":"d4-p10","domain":"D4","title":"Force tool-call order with tool_choice","explanation":"When a pipeline tool is sometimes called before a prerequisite tool it depends on, force the correct one first with tool_choice for that turn, then switch back to \"auto\" for subsequent turns — don't rely on the order tools are listed in."},
  {"id":"d4-p11","domain":"D4","title":"An evolving enum needs an \"other\" escape hatch","explanation":"When new categorical values keep appearing, add an \"other\" enum value plus a separate detail string field — this keeps strict validation for common cases while avoiding endless enum maintenance or the validation loss of switching to free text."},
  {"id":"d4-p12","domain":"D4","title":"Field-level confidence to prioritize limited review capacity","explanation":"When reviewers can only check a fraction of extractions, have the model output field-level confidence scores and calibrate the review threshold using a labeled validation set — this concentrates reviewer attention on errors far more effectively than random sampling."},
  {"id":"d5-p1","domain":"D5","title":"The deciding factor for using the Batch API","explanation":"The Batch API saves 50% but can take up to 24 hours. The core question is always whether results arriving up to 24 hours later remain actionable for your workflow — not result ordering or whether a request needs multi-turn refinement."},
  {"id":"d5-p2","domain":"D5","title":"Calculating a safe batching cadence","explanation":"Worst-case wait-to-be-batched plus the batch processing SLA (up to 24 hours) must fit comfortably within your overall SLA, with a safety margin for reliability."},
  {"id":"d5-p3","domain":"D5","title":"context_length_exceeded errors: fix only the failed subset","explanation":"When a small percentage of documents in a large batch fail due to exceeding the context length, the cost-effective fix is to chunk and resubmit only those failed documents, then merge the partial results — not reprocessing the entire batch, and not increasing max_tokens."}
]
;
